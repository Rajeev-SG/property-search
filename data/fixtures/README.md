# Fixtures

Synthetic starter fixtures to seed evals and schema validation.

Use this directory to store:
- raw HTML pages
- extracted markdown or JSON
- expected canonical outputs
- per-domain gold examples
